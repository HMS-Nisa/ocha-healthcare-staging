---
// src/layouts/LandingLayout.astro
import { ShieldCheck, Search } from 'lucide-react';
import { WA_NUMBER } from '../config'; 

const { title, description } = Astro.props;
const waLink = `https://wa.me/${WA_NUMBER}?text=Hi Ocha, I need assistance with medical arrangements.`;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<title>{title}</title>
		<link href="https://api.fontshare.com/v2/css?f[]=satoshi@900,700,500,400&f[]=clash-display@400,500,600,700&display=swap" rel="stylesheet">
	</head>
	<body class="bg-slate-50 font-sans text-slate-900 antialiased flex flex-col min-h-screen">
		
		{/* 🚀 NAVBAR START */}
		<header class="sticky top-0 w-full bg-slate-50/80 backdrop-blur-md z-50 border-b border-slate-100/50">
			<div class="max-w-7xl mx-auto px-6 h-20 relative flex items-center justify-between">
				
				{/* 1. LEFT: Logo (Relative + z-index to sit on top) */}
				<div class="relative z-20 flex-shrink-0">
					<a href="/" class="flex items-center gap-2">
						<img src="/assets/logo.jpg" alt="Ocha Healthcare" class="h-10 w-auto mix-blend-multiply" />
					</a>
				</div>

				{/* 2. CENTER: Menu Links (ABSOLUTE FULL WIDTH) */}
				{/* "inset-0" stretches this box to fill the whole header */}
				{/* "justify-center" locks text in the exact middle */}
				{/* "pointer-events-none" ensures invisible parts don't block clicks on logo/buttons */}
				<nav class="hidden md:flex absolute inset-0 w-full h-full items-center justify-center pointer-events-none z-10">
					<div class="flex items-center gap-8 font-medium text-slate-600 pointer-events-auto">
						<a href="/" class="hover:text-[#276CA1] transition-colors text-sm font-bold">Home</a>
						<a href="/directory" class="hover:text-[#276CA1] transition-colors text-sm font-bold">Doctors</a>
						<a href="/blog" class="hover:text-[#276CA1] transition-colors text-sm font-bold flex items-center gap-2">
							Guides 
							<span class="bg-blue-100 text-[#276CA1] text-[10px] px-2 py-0.5 rounded-full font-extrabold uppercase tracking-wide">New</span>
						</a>
					</div>
				</nav>

				{/* 3. RIGHT: Actions (Relative + z-index to sit on top) */}
				<div class="relative z-20 flex-shrink-0 flex items-center gap-3">
					
					{/* Search Button */}
					<a href="/directory" class="hidden lg:flex items-center gap-2 px-4 py-2 bg-white border border-slate-200 rounded-full text-sm font-semibold text-slate-600 hover:border-[#276CA1] hover:text-[#276CA1] transition-all">
						<Search className="w-4 h-4" />
						<span>Find Specialist</span>
					</a>

					{/* CTA Button */}
					<a 
						href={waLink} 
						target="_blank" 
						class="px-5 py-2.5 bg-[#276CA1] text-white font-bold rounded-full text-sm hover:bg-[#1f5682] transition-colors shadow-lg shadow-blue-900/10"
					>
						Book Consultation
					</a>
				</div>

			</div>
		</header>
		{/* 🚀 NAVBAR END */}

		<slot />

		<footer class="bg-white border-t border-slate-100 py-8 mt-auto">
			<div class="max-w-7xl mx-auto px-6 text-center text-slate-400 text-sm">
				<div class="flex items-center justify-center gap-2 mb-4 text-emerald-600 font-medium">
					 <ShieldCheck className="w-4 h-4" />
					 <span>Your data is secure. We never spam.</span>
				</div>
				<p>© {new Date().getFullYear()} Ocha Healthcare. Not a medical provider.</p>
			</div>
		</footer>
	</body>
</html>